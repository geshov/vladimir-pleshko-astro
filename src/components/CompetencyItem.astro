---
const { name, percent } = Astro.props;
---

<div class="flex flex-col gap-0">
  <label class="text-sm font-medium">{name}</label>
  <progress class="progress progress-accent h-3" value={percent} max="100"
  ></progress>
</div>
